# Deployment

TBD in Phase 8 (CI/CD And Deployment).

Target topology (see `docs/architecture.md`): Firebase Hosting for `apps/game`, Cloud Run for `services/api`, Firestore for persistence, GitHub Actions for CI/CD. Will document environment configuration, secrets management, and the release process once deploy workflows exist.
