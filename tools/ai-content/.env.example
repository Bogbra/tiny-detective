# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# Required by generate_cases.py and evaluate_cases.py (the case generator and
# the AI-judged logic/safety evaluators). Not needed to run `uv run pytest` —
# the automated tests use fake evaluators. See ai_content/openai_client.py.
OPENAI_API_KEY=
