# Contracts

Shared API contracts between `services/api` and `apps/game`: the OpenAPI schema and generated Dart DTOs derived from it.

Populated once the backend API surface exists (Phase 3) — the OpenAPI schema is generated from `services/api`, and the Dart client types are generated into `generated/` from that schema.
